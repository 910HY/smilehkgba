{
  "version": 2,
  "buildCommand": "bash vercel-build.sh && cd client && npm install && npm run build && cp -r dist/* ../dist/",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "framework": null,
  "public": true
}